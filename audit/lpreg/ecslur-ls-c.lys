// Lily# twin of ecslur-c.ly: the control the LP oracle says (a) must equal —
// the slur closed on the note that occupies the empty chord's moment.
octave absolute
time 4/4

part melody

section A {
  melody {
    r4 e8( g c'4) r4 |
  }
}

form main { ~A }

score main { staff melody }
