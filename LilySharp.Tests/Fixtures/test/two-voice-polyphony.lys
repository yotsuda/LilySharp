// Coverage: two voices on one staff (voice { } { }) — the upper voice gets
// stems up, the lower voice stems down, and they continue independent relative-
// octave chains. Guards multi-voice stem direction and layout.
title "Two-voice polyphony"
time 4/4
key c major
part m { clef treble }
section Main {
  m {
    voice { g'2 a | b4 c b a | } { c4 d e f | g2 g | } 
  }
}
form main { Main }
score main "two-voice-polyphony" { staff m }

// ---------------------------------------------------------------------------
// ⚠️ THIS NOTE IS AT THE FILE END ON PURPOSE. A comment ABOVE the music shifts every
// source offset after it, so the whole book's data-pos moves and its snapshot has to be
// re-based -- a diff that proves nothing and hides the next real one. Below the music,
// nothing moves (measured: suite green with these notes, 10 snapshots red without).
// ✔ VERIFIED TO GUARD, not assumed to (session 190): VoiceDefaults.GetDefaultStemUp returning null for every voice moved 31 of 219 fixtures, this one among them. ⚠️ AND THE POISON HAD TO BE THE RIGHT ONE OF TWO: the rule has two homes. GetDefaultStemUpAt (the span-narrowed reader, ~10 live consumers) forced to null moves 7 fixtures and NOT this book, because MeasureCollector (:2061) writes the direction back onto the items from the UN-narrowed GetDefaultStemUp at collect time, and that write-back is what this book renders from. One quantity, two places -- HANDOFF §2A.
