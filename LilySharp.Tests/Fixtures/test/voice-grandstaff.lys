// Intra-staff polyphony on a grand staff: the upper staff splits into two
// voices for one measure (voice { } { }) while staying single-voice before and
// after, and the lower staff is plain. BOTH voices of the span must render
// (regression guard for "grand-staff staff shows the primary voice only").
title "Grand Staff Voices"
composer "Lily#"
tempo 90
time 4/4
key c major

part rh { clef treble }
part lh { clef bass }

section Main {
  rh { c'4 d e f | voice { g'2 a } { c2 d }  | e'4 d c b | }
  lh { c4 d e f | g4 a g f | e4 d c2 | }
}

form main { Main }

score main "voice-grandstaff" {
  grandStaff {
    staff rh
    staff lh
  }
}

// ---------------------------------------------------------------------------
// ⚠️ THIS NOTE IS AT THE FILE END ON PURPOSE. A comment ABOVE the music shifts every
// source offset after it, so the whole book's data-pos moves and its snapshot has to be
// re-based -- a diff that proves nothing and hides the next real one. Below the music,
// nothing moves (measured: suite green with these notes, 10 snapshots red without).
// ✔ VERIFIED TO GUARD, not assumed to (session 190): VoiceDefaults.GetDefaultStemUp returning null moved this book (31 of 219 moved), so the span's second voice really is rendered and placed here.
