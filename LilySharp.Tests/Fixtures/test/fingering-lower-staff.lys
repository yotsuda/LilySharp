// Fingerings live on the NoteItem, so they must be read from EACH staff's own
// voice and positioned at that staff's index. Authored on the SECOND (bass)
// staff, they previously vanished (read only from the first staff, and the Y
// double-counted the system origin). Verified against LilyPond. Guards the
// per-staff FingeringEngraver pass.
title "Lower-staff fingering"
time 4/4
key c major

part top { clef treble }
part bot { clef bass }

section Main {
  top { c'4 d e f | g1 | }
  bot { c4@finger(1) d@finger(2) e@finger(3) f@finger(5) | g1@finger(1) | }
}

form main { Main }

score main "fingering-lower-staff" {
  staff top
  staff bot
}

// ---------------------------------------------------------------------------
// ⚠️ THIS NOTE IS AT THE FILE END ON PURPOSE. A comment ABOVE the music shifts every
// source offset after it, so the whole book's data-pos moves and its snapshot has to be
// re-based -- a diff that proves nothing and hides the next real one. Below the music,
// nothing moves (measured: suite green with these notes, 10 snapshots red without).
// ✔ VERIFIED TO OBSERVE FINGERINGS, not assumed to (session 190): FingeringEngraver.Calculate returning empty moved 5 of 219 fixtures, this one among them. ⚠️ The STAFF-ROUTING claim below is still unmeasured — session 183 tried three routing poisons and none moved a book, and the poison that works here removes fingerings outright, which cannot tell the staves apart.
