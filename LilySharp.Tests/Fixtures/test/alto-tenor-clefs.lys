// Coverage: alto and tenor C-clefs (the C-clef sits on a different line in each,
// so notes and the clef glyph land at different staff positions). Guards the
// per-clef staff-position cases in CalculateStaffPosition.
title "Alto and tenor clefs"
time 4/4
key f major
part alt { clef alto }
part ten { clef tenor }
section Main {
  alt { c4 d e f | g a bes c | }
  ten { c4 d e f | g a bes c | }
}
form main { Main }
score main "alto-tenor-clefs" { staff alt staff ten }

// ---------------------------------------------------------------------------
// ⚠️ THIS NOTE IS AT THE FILE END ON PURPOSE. A comment ABOVE the music shifts every
// source offset after it, so the whole book's data-pos moves and its snapshot has to be
// re-based -- a diff that proves nothing and hides the next real one. Below the music,
// nothing moves (measured: suite green with these notes, 10 snapshots red without).
// ✔ VERIFIED TO GUARD, not assumed to (session 190): dropping the "alto"/"tenor" cases from MeasureCollector.ItemFactory's basePosition switch (the exact fall-through this names) moved 5 of 219 fixtures, this one among them.
