// Polyrhythm: the upper voice has a triplet (stems forced up), the lower voice
// four straight quarters. The tuplet "3" bracket must sit ABOVE the triplet's
// three noteheads — not below by the lower voice (regression guard: the bracket
// used the pitch-default stem side, which put it on the wrong side in a voice
// whose stems are force-flipped, making the triplet look like it lost a note).
title "Polyrhythm Tuplet"
composer "Lily#"
tempo 90
time 4/4
key c major

part m { clef treble }

section Main {
  m { voice { tuplet 3/2 { c'4 d e } f2 } { e4 e e e }  | }
}

form main { Main }

score main "voice-tuplet" {
  staff m
}

// ---------------------------------------------------------------------------
// ⚠️ THIS NOTE IS AT THE FILE END ON PURPOSE. A comment ABOVE the music shifts every
// source offset after it, so the whole book's data-pos moves and its snapshot has to be
// re-based -- a diff that proves nothing and hides the next real one. Below the music,
// nothing moves (measured: suite green with these notes, 10 snapshots red without).
// ✔ VERIFIED TO GUARD, not assumed to (session 190): VoiceDefaults.GetDefaultStemUp returning null moved this book (31 of 219 moved). ⚠️ The narrowed GetDefaultStemUpAt does NOT move it -- see two-voice-polyphony.lys for why (the collector writes the direction back onto the items from the un-narrowed rule).
