# Lily#

**セッション開始時に `docs/HANDOFF.md` を読むこと。** 現在地・開いている作業・決定済み事項・
恒久ルール・コマンド集はそこにある。過去のセッション記録は `docs/HANDOFF-ARCHIVE.md`
（**同じ regime に触るときだけ**読む）。**引継ぎ用の新しい `handoff-*.md` を作らない**
（root に残る旧方式の残骸は `docs/HANDOFF.md` §8 参照）。

以下は、外すと実害が出るものだけの抜粋。詳細は `docs/HANDOFF.md`。

## 絶対に外さないこと

- **`docs/HANDOFF.md` §1 の「現在地」は書いた時点のスナップショット。**
  HEAD・テスト数・シンボル名・「完了」表記は**開始時に実コードで裏取りする**
- **push はユーザー。**「done」は push 済みでのみ主張。ship = 全緑 ＋ 明示承認
- **出力を変える変更はユーザー承認前に出荷しない。**
  snapshot 再ベースも **LP 照合 → 承認 → 実行**
- **master 直コミット。ブランチを勝手に作らない**
- **Co-Authored を付けない**
- **シンボルのリネームはユーザーが MSVS で行う**（Claude は自分で改名しない。ファイルパス・
  行番号・新旧の名前を伝える。新規追加と削除は対象外＝削除はユーザー承認のうえ Claude が行う）
- **巨大ファイルを分割しない**

## 実装するとき

> ## ★ 実測値に基づいてコードを直すことは禁止
> **コードに入るのは LP の `lily/*.cc` / `scm/*.scm` の式だけ。** LilyPond の出力から読んだ
> 数値でコードを書いてはならない。実測の役割は 2 つだけ＝**①欠陥を見つけるヒント
> ②移植の照合**。判定法: その値を **LP のどの関数のどの行から導いたか**をコメントに書けるか。
> 書けないなら `LILYSHARP-OWN:` を付けて独自と明示するか、式を探しに戻る。

> ## ★ byte 一致を維持するための細工も禁止
> **目標は出力の一致ではなく、LP の内部ロジックの完全模倣。** 出力を動かさないために
> LP に無い分岐・除外・条件・近似を入れてはならない。判定法: **その分岐/除外は LP のどの行にあるか。**
> よく出る形＝「両側から同じものを除外して一致したと言う」「既存 snapshot が動かないほうを選ぶ」
> 「今のデータ構造では LP の量を表現できないから除外する」（表現できるように直すのが移植）。
> **byte 不変は「結果」ならよいが「構成」にしてはいけない**——報告にはどちらかを書く。
> 逆に**観測可能な差が無いことは字面移植をしない理由にならず**、byte 不変は移行が正しい証拠にもならない。

- レイアウト/描画は `C:\MyProj\lilypond-src` の `lily/*.cc` を**符号一致で字面移植**。
  独自の近似を入れない。移植したら **`// LILYPOND-REF: lily/xxx.cc:行`** を必ず付ける
- **座標系が揃っていなくて字面移植が難しいときは、押し込まず報告する**
- **doc・コメント・過去の結論を疑う。ただし疑った結果も裏取りする**
  （`LILYPOND-REF` があっても式が一致しているとは限らない）
- **同じ量を計算する場所が 2 つ以上あったら、そこが次の欠陥の住所**（予約と描画・
  レイアウトと改行器）。片方だけ直すと欠陥は生き残る
- **推論せず測る。** LP 幾何の精密測定に SVG を使わない（座標が 2 桁に丸められる）。
  `LilySharp.Tests/LpFidelity/` の記録用コンテキストを使う
- **台帳の点は必ず「対」で起票し、予測を先に `why` へ書く**（`docs/HANDOFF.md` §5.0）。
  片側だけだと予測が当たってしまい、欠陥ごと出荷する

## 環境

- **シェルは pwsh MCP / ripple（bash 禁止）。** ファイル書き込みは Write ツール
  （PowerShell に heredoc は無い。commit message はファイルに書いて `git commit -F`）
- **`dotnet` の増分ビルドが腐る** → 前後比較は `--no-incremental` でビルドして `--no-build` で実行
- **LilyPond は Guile デッドロックする** → `cmd /c "... < NUL"` でデタッチ必須

## 品質指標

`audit/lp-geometry/` の **LP 忠実度台帳**が LP との距離を数値で持つ。
snapshot は「前回の自分」との比較なので誤りが固定されうる。台帳は LP との比較で、
**残差が増えても減ってもテストが落ちる**（改善も意図的に記録させるため）。

⚠️ **total を過去の値と直接比べない**（点集合が違う）。増加は悪化とは限らず、
**誰も測っていなかった乖離を可視化しただけ**のことが多い。

```powershell
dotnet test LilySharp.Tests\LilySharp.Tests.csproj --no-build `
  --filter 'FullyQualifiedName~Corpus_ReportsTotalDivergence' --logger 'console;verbosity=detailed'
```
