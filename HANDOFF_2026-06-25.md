# Lily# 引継ぎ (2026-06-25)

## 現在の未解決issue（最優先）
**`07-figbass-chordname-lower-staff.png` のコードネームが下にズレてト音五線と重なる。**

- フィクスチャ: `LilySharp.Tests/Fixtures/test/figbass-chordname-lower-staff.lys`
  - `bot { c4@chord.Cm7 d@fig.6 e@fig.6.4 f | g1@chord.G7 | }`（2譜表: top=treble, bot=bass）
- 症状: バス譜表に付けたコードネーム `Cm7`/`G7` が、**ト音譜表の最下線あたりに表示され五線と重なる**。本来は五線と重ならない位置に出すべき。
- 図形バス `6` / `6/4` は正しくバス譜表の下に出ている（問題なし）。
- 関連コード: `LilySharp.Core/Svg/Layout/ChordNameEngraver.cs` — per-staff 修正で `double y = -StaffPadding + staffOffset;`（`staffOffset`=staffYByIndex[StaffIndex]）。`SharedRenderer` の DrawChordNames も確認。
- 調査途中: 07 を `svg` サブコマンドで出力し、コードネーム text の Y と五線 y を比較して staffOffset が効いているか確認する手前で中断。
- 仮説: chord name を「自分の譜表(バス)の上」に置くと、ちょうどト音五線の最下線と重なる位置になる。LP ではコードネームは通常**システム最上譜表の上**に出る。per-staff にした判断自体を見直す価値あり。**実 LilyPond で ChordNames の配置を確認してから直すこと（C:\MyProj\lilypond-src）。**

## このセッションで完了（すべて commit+push 済み、CI 緑、最新 ~`95443a9`）
多譜表で「下段譜表の per-note 注釈が第1譜表に固定/消失」バグクラスを全面修正。詳細はメモリ `project_lilysharp_multistaff_annotation_staffindex.md`。

- Dynamics（譜表間スカイライン＋per-staff routing）
- Articulations+Ornaments / Tuplet brackets / Arpeggios / Figured bass+Chord names / Grace notes / Trill spanners / Fingerings — 各々 `StaffIndex` タグ付け＋エングレーバー per-staff 化、回帰フィクスチャ `test/*-lower-staff`
- Pedal の X アンカー（音符列に。`MusicMarkItem.AnchorTiming` 捕捉）
- `lyrics-verses` CI 失敗修正（作業ツリー CRLF→LF 正規化＋snapshot 再生成。data-pos=バイトオフセット）
- **譜表間スペーシング: スカイラインに StaffSymbol(±StaffHeight/2)を seed**（`SkylineBuilder.SeedStaffSymbol`、回帰 `test/grandstaff-high-bass`）。高い下段和音が上段五線を突き抜ける件を LP 準拠で修正。

作業ツリーはクリーン（デバッグ除去済み）。全テスト基準 1543 passed / 3 skipped / 0 failed。

## 「改善していない」と言われた件の結論（allann）= バグではない
- 相対オクターブ累積でバス声部が C5–G5 まで上昇（`<<>>`後の参照が上声部最終音→m3 が C5 起点）。
- SVG 実測: バス和音最上音 y=20.14 / ト音最下線 y=18.64 → **約1.0 staff-space 下でクリア（線は越えない）**。
- 実 LilyPond（`scratchpad\allann-lp.png` / `allann-lp.ly`）で同音符 → **LP も同等に詰める**＝LP 準拠。staff-symbol seed は allann には不影響（隙間は m2 が決定）、minrepro 等の真の突き抜けには有効。

## レビュー用ファイル
`C:\MyProj\LilySharp\output\review\session-fixes\` に13項目 ×（.lys/.png/.svg/.pdf）。全部現コードで再生成済み。
00-all-annotations / 01-voices-dynamics-clefs / 02-dynamics-interstaff-spacing / 03-dynamics-lower-staff / 04-articulations-lower-staff / 05-tuplet-lower-staff / 06-arpeggio-lower-staff / **07-figbass-chordname-lower-staff(要修正)** / 08-grace-lower-staff / 09-trillspan-lower-staff / 10-fingering-lower-staff / 11-pedal-note-anchor / 12-grandstaff-high-bass-spacing

## ビルド/検証の必須事項（gotcha）
- **CLI レンダリングは先に `dotnet build LilySharp.Cli/LilySharp.Cli.csproj -c Debug`。** Core.csproj 単独＋`--no-build` だと CLI bin の古い Core.dll を使い変更が反映されない。`dotnet test` は自動リビルドで安全。
- 生成: `dotnet run --project LilySharp.Cli -c Debug --no-build -- png --scale 2.5 <in.lys> <out.png>`（svg/pdf も同様、png のみ --scale）。私の環境に pdftoppm 無く PDF を画像化表示できない。
- スナップショット更新: `LILYSHARP_UPDATE_SNAPSHOTS=1` で `dotnet test --filter "FullyQualifiedName~SvgSnapshotTests"`。**snapshot は構文エラーを通して描画→PNG 目視必須。**
- シェルは Bash/PowerShell ツール禁止。`mcp__ripple__execute_command(shell=pwsh)`。
- `.lys` は LF 固定（.gitattributes `*.lys text eol=lf`）。CRLF だと CI(Linux) で data-pos 不一致。
- パート名に `p`/`f` 等は dynamic と衝突するので避ける（`lh`/`top` 等）。
- 相対オクターブのズレは大半が記譜ミス→PNG 自分で目視してから見せる。アドホックなパッチでなく LP ロジックを忠実模倣。

## 参照コード
- LP ソース: `C:\MyProj\lilypond-src`（`lily/axis-group-interface.cc` VAG skyline は StaffSymbol 含む 914-940 / `lily/skyline.cc` / `scm/define-grobs.scm`）
- Lily# スペーシング: `EngravingDefaults.cs` StaffStaffBasicDistance=9.0/Minimum=7.0/Padding=1.0。`MultiStaffLayouter.CalculateStaffGapWithSkylines` / `SkylineBuilder.cs` / `VerticalSkyline.cs`(Distance=h1+h2, UP=-yTop/DOWN=yBottom)
- 注釈レイアウト: `LayoutEngine.CalculateAnnotationLayouts`（多譜表 ~line396-435 で voicesByStaff/measuresByStaff/staffYByIndex 構築）/ `SharedRenderer.cs`(page-level overlays= sysY+layout.Y)
